configFile: /etc/ceph/ceph.conf
pool:
  "rbd":
    diskType: SSD
    AZ: ceph
    recoveryTimeObjective: 0
    provisioningPolicy:
    - thin
    - fixed
    accessProtocol: rbd
    maxIOPS: 1000
    compress: true
    dedupe: false
